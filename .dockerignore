node_modules
**/node_modules
**/dist
**/coverage
.git
.github
docs
*.md
.env
.env.*
!.env.example
.deploy
postgres-data
redis-data
uploads
backups
**/*.tsbuildinfo

# The 1.3 MB brand master (docs/brand.md §1). No build step reads it — the
# derived WebPs are committed under `apps/*/public/brand/` — so shipping it into
# the build context is a megabyte over the wire on every uncached build for
# nothing.
logo.png
